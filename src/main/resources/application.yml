server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blogging
    username: root
    password: Ashu@123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
